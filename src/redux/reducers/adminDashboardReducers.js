import studentAttendenceData from "../../fakeData/adminData/studentAttendenceData.json";
const primaryState = {
  studentAttendenceData: studentAttendenceData,
};

const adminDashboardReducers = (state = primaryState, action) => {
  return state;
};

export default adminDashboardReducers;
